import React, { FC } from 'react';
import { Box } from '@mui/material';

const Series: FC = () => {
  return (
    <Box>
      <div>
        <p>Series</p>
      </div>
    </Box>
  );
};

export default Series;
